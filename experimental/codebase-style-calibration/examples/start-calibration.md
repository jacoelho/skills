# Start calibration prompt

Use the `codebase-style-calibration` skill.

Inspect this repository and start Round 1.

Show 3 to 6 probes using a mix of:

- `REAL` snippets from the codebase
- `MUTATION` snippets that minimally alter real code
- `GENERATED` snippets that imitate plausible future AI-generated code

For each probe, explain what preference it tests and ask concrete review questions.

Do not produce the final style guide yet.

At the end, list preliminary suspected rules as low-confidence drafts.
